# 連線節點建議（網頁版）

一個可以在 **Safari 直接開、免登入、可加到主畫面**的單頁工具。
它量測你當下的網速與到各國的延遲，然後算出應該連哪一國的節點，
並產生對應的 pyvpn 客戶端設定。

## 它做什麼、不做什麼

| | |
|---|---|
| ✅ 量測對外 IP、出口國家、最近的 CDN 節點 | 透過 `speed.cloudflare.com/cdn-cgi/trace` |
| ✅ 量測真實下載速度 | 實際下載 2–20 MB 並計時 |
| ✅ 量測到 10 個國家的延遲 | 對各地區實體機房的端點計時，非 anycast |
| ✅ 依網速判斷可接受哪些國家 | 速度越慢，可容忍的延遲差距越小 |
| ✅ 產生 pyvpn 設定 | 一鍵複製 |
| ❌ **建立 VPN 連線** | 網頁做不到，見下方 |

### 為什麼網頁不能自己連 VPN

iOS 只允許取得 **NetworkExtension** 權限的原生 App 接管系統流量。
Safari 沒有這個 API，也沒有任何 JavaScript 介面可以建立隧道或改路由。
所以任何宣稱「在瀏覽器裡開 VPN」的網頁都做不到——它們實際上多半只是
把某個網站的內容轉一手的網頁代理，跟 VPN 不是同一件事。

實際的連線由本專案的 `pyvpn` 客戶端負責。

## 網址

站台已經上線，不需要登入就能開：

**https://nfjnjrhfr.github.io/fake-minecraft-/**

### 它是怎麼發佈的

Pages 從 `gh-pages` 分支的根目錄供應檔案。推出那個分支這件事本身就讓
GitHub 啟用了 Pages，所以沒有人需要去改任何設定。

`.github/workflows/pages.yml` 維持這個狀態：只要 `docs/` 有變動被推上
`main` 或開發分支，它會先跑 `logic.test.mjs`，通過才把 `docs/` 的內容複製
到 `gh-pages` 的根目錄並強制推送。那個分支只放產生出來的檔案，所以整個覆蓋
就是原意。

改網站的時候改 `docs/`，不要直接動 `gh-pages` —— 下一次發佈會蓋掉它。

> Pages 走 HTTPS，這是必要的：Service Worker 與「加入主畫面」的全螢幕模式
> 在 `http://` 或 `file://` 底下都不會生效。

## 量測哪些國家

清單是 Proton VPN 免費方案能連的 10 個國家。列出連不到的地方沒有意義 ——
建議了也選不了。

延遲用 AWS 各區域的端點量測。有幾個國家沒有自己的 AWS 區域，就借用鄰近
國家的節點，這些在畫面上標 `≈`，並且**共用同一個量測點的國家會拿到相同
的數字**（荷蘭、波蘭、羅馬尼亞都用法蘭克福）。每個端點只量一次再分配，
不重複量同一台伺服器。

| 國家 | 量測點 | |
|---|---|---|
| 🇯🇵 日本 | 東京 | 準確 |
| 🇸🇬 新加坡 | 新加坡 | 準確 |
| 🇺🇸 美國西岸 | 奧勒岡 | 準確 |
| 🇺🇸 美國東岸 | 維吉尼亞 | 準確 |
| 🇨🇦 加拿大 | 蒙特婁 | 準確 |
| 🇲🇽 墨西哥 | 墨西哥市 | 準確 |
| 🇨🇭 瑞士 | 蘇黎世 | 準確 |
| 🇳🇱 荷蘭 | 法蘭克福 | ≈ 估計 |
| 🇵🇱 波蘭 | 法蘭克福 | ≈ 估計 |
| 🇷🇴 羅馬尼亞 | 法蘭克福 | ≈ 估計 |
| 🇳🇴 挪威 | 斯德哥爾摩 | ≈ 估計 |

換成別家 VPN 時，改 `index.html` 裡的 `REGIONS` 就好。測試會直接從那份清單
讀取，所以不會跟程式脫節。

## 加到主畫面

在 Safari 打開網址後：

| 裝置 | 分享鍵在哪 |
|---|---|
| **iPad** | **右上角**，網址列旁邊 |
| iPhone | 下方工具列正中間 |

按分享鍵 → 往下捲 → **加入主畫面** → **新增**。

之後點桌面上的圖示就會**全螢幕**打開（沒有網址列、沒有分頁列），
而且一開啟就自動開始量測，不用再按任何按鈕。

頁面本身會依裝置顯示正確的位置，所以照畫面上寫的做就好。

## 本機預覽

```bash
cd docs && python3 -m http.server 8000
# 然後開 http://localhost:8000
```

Service Worker 在 `localhost` 也算安全來源，所以離線快取一樣可以測試。

## 檔案

| 檔案 | 用途 |
|---|---|
| `index.html` | 節點建議 App，CSS 與 JS 都內嵌，沒有外部相依 |
| `build-source-page.py` | 從 `pyvpn/*.py` 產生 `server.html`，用 stdlib 的 `tokenize` 上色 |
| `server.html` | 伺服器原始碼閱讀頁（產生物，不進版控，發佈時重新產生） |
| `browser.test.py` | 真實瀏覽器的端對端測試，含 iPhone / iPad 版面檢查 |
| `logic.test.mjs` | 建議演算法的測試，函式直接從 `index.html` 取用 |
| `manifest.webmanifest` | PWA 設定：名稱、圖示、`display: standalone` |
| `sw.js` | Service Worker，快取 App 外殼；量測請求一律不快取 |
| `icon.svg` / `icon-*.png` | 圖示；`icon-180.png` 是 iOS 主畫面用的尺寸 |

## 量測方式

**延遲**：對各地區的 AWS S3 端點（`s3.<region>.amazonaws.com`）發出
`mode: "no-cors"` 的請求並計時。這些端點固定在該地區的機房，所以往返時間
真的反映到那個國家的距離；用 anycast 的主機則永遠只會回答你最近的節點，
量不出差別。每個地區先做一次暖身（DNS 與 TLS），再取三次的最小值。

**速度**：先下載 2 MB，若判定線路夠快再下載 20 MB，並從**首個位元組抵達後**
開始計時，把連線建立的成本排除在外。

**建議**：

```
可容忍延遲 = 10 ms + min(下載速度, 100 Mbps) × 0.8
```

延遲在「最佳值 + 可容忍延遲」以內的國家都標為可用。
線路慢的時候這個窗口很窄（1 Mbps 大約只有 11 ms），所以只有最近的節點可選；
線路快的時候窗口寬（100 Mbps 大約 90 ms），你就可以照自己想要的出口國家挑。
